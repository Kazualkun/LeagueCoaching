"""Provedores de LLM. Adicionar um e implementar uma classe.

A interface esta em llm/base.py (protocolo `Provider`): dois metodos,
`complete` e `healthy`. Ela e pequena de proposito — "adicione um provedor
gratuito" e uma das boas primeiras issues do projeto, e ela so continua boa se
a superficie for esta.
"""
